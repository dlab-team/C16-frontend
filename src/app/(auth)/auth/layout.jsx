

export default function layout({ children }) {
    return (
        <>
            <header>Auth navbar</header>
            <main>{children}</main>
            <footer>Auth navbar</footer>

        </>
    )
}
